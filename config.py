# Network configuration for connecting to the AI Backend Server

# The IP address of the backend server. 
# "127.0.0.1" (or "localhost") means the server is running on the SAME computer as this UI.
SERVER_HOST = "127.0.0.1" 

# The Port number. Think of this as the specific "door" the backend is listening on.
# (We assume the backend team set their server to listen on port 5050)
SERVER_PORT = 5050